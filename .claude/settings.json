{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *README.md) printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"README.md was just modified. Project rule: every README update must be run through the humanizer skill. Review the changed README text with the humanizer skill now and fix any AI-writing patterns before finishing.\"}}' ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
