{
  "name": "ryans-place",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Ryan's Place — a walkable pixel-art world that doubles as a portfolio.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
